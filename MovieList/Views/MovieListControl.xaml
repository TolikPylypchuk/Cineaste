<UserControl x:Class="MovieList.Views.MovieListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:resx="clr-namespace:MovieList.Properties"
             xmlns:vm="clr-namespace:MovieList.ViewModels" mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:MovieListViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="500" d:DesignWidth="500" Loaded="MovieListControl_Loaded">
    <UserControl.Resources>
        <Style x:Key="TextBlockStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Margin" Value="2,5" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>
        
        <Style x:Key="NumberTextBlockStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource TextBlockStyle}">
            <Setter Property="TextAlignment" Value="Left" />
            <Setter Property="Margin" Value="10,2,2,2" />
        </Style>

        <Style x:Key="YearTextBlockStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource TextBlockStyle}">
            <Setter Property="TextAlignment" Value="Right" />
            <Setter Property="Margin" Value="10,2,5,2" />
        </Style>
    </UserControl.Resources>
    
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="{x:Static resx:Resources.Title}" FontSize="36"
                   TextAlignment="Center" HorizontalAlignment="Stretch" Margin="5" />

        <Border BorderBrush="Black" BorderThickness="1" Margin="5" HorizontalAlignment="Stretch">
            <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Visible">
                <HeaderedItemsControl ItemsSource="{Binding Path=Items}" HorizontalContentAlignment="Stretch"
                                      HorizontalAlignment="Stretch" Margin="0,0,5,0">
                    <HeaderedItemsControl.Template>
                        <ControlTemplate TargetType="HeaderedItemsControl">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                                <DockPanel>
                                    <ContentPresenter DockPanel.Dock="Top" ContentSource="Header"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                    <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </DockPanel>
                            </Border>
                        </ControlTemplate>
                    </HeaderedItemsControl.Template>

                    <HeaderedItemsControl.HeaderTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50" SharedSizeGroup="First" />
                                    <ColumnDefinition Width="*" SharedSizeGroup="Second" />
                                    <ColumnDefinition Width="*" SharedSizeGroup="Third" />
                                    <ColumnDefinition Width="100" SharedSizeGroup="Fourth" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{x:Static resx:Resources.ListItemNumber}"
                                    Style="{StaticResource NumberTextBlockStyle}" />

                                <TextBlock Grid.Column="1" Text="{x:Static resx:Resources.ListItemTitle}"
                                    Style="{StaticResource TextBlockStyle}" />

                                <TextBlock Grid.Column="2" Text="{x:Static resx:Resources.ListItemOriginalTitle}"
                                    Style="{StaticResource TextBlockStyle}" />

                                <TextBlock Grid.Column="3" Text="{x:Static resx:Resources.ListItemYear}"
                                    Style="{StaticResource YearTextBlockStyle}" />
                            </Grid>
                        </DataTemplate>
                    </HeaderedItemsControl.HeaderTemplate>

                    <HeaderedItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50" SharedSizeGroup="First" />
                                    <ColumnDefinition Width="*" SharedSizeGroup="Second" />
                                    <ColumnDefinition Width="*" SharedSizeGroup="Third" />
                                    <ColumnDefinition Width="100" SharedSizeGroup="Fourth" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding Path=DisplayNumber}"
                                       Style="{StaticResource NumberTextBlockStyle}" />

                                <TextBlock Grid.Column="1" Text="{Binding Path=Title}"
                                       Style="{StaticResource TextBlockStyle}" />

                                <TextBlock Grid.Column="2" Text="{Binding Path=OriginalTitle}"
                                       Style="{StaticResource TextBlockStyle}" />

                                <TextBlock Grid.Column="3" Text="{Binding Path=Year}"
                                       Style="{StaticResource YearTextBlockStyle}" />
                            </Grid>
                        </DataTemplate>
                    </HeaderedItemsControl.ItemTemplate>
                </HeaderedItemsControl>
            </ScrollViewer>
        </Border>
    </DockPanel>
</UserControl>
