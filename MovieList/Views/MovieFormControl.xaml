<UserControl x:Class="MovieList.Views.MovieFormControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:MovieList.Views"
             xmlns:conv="clr-namespace:MovieList.Converters"
             xmlns:resx="clr-namespace:MovieList.Properties"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:MovieList.ViewModels"
             xmlns:vmf="clr-namespace:MovieList.ViewModels.FormItems" mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:MovieFormViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="800" d:DesignWidth="300">
    <UserControl.Resources>
        <Style x:Key="InputTitleStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="DockPanel.Dock" Value="Left" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Margin" Value="0,2" />
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Width" Value="21" />
            <Setter Property="Height" Value="21" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="DockPanel.Dock" Value="Right" />
            <Setter Property="Margin" Value="0,2" />
            <Setter Property="Padding" Value="0" />
        </Style>

        <Style x:Key="AddButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Content" Value="{x:Static resx:Messages.AddIcon}" />
            <Setter Property="VerticalAlignment" Value="Bottom" />
        </Style>

        <Style x:Key="RemoveButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Content" Value="{x:Static resx:Messages.RemoveIcon}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="2,2,0,2" />
        </Style>

        <Style x:Key="InputTextBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="0,2" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="MaxLines" Value="15" />
            <Setter Property="local:Properties.TripleClickSelectAll" Value="True" />
        </Style>

        <Style x:Key="UrlInputTextBoxStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource InputTextBoxStyle}">
            <Setter Property="TextWrapping" Value="NoWrap" />
            <Setter Property="MaxLines" Value="1" />
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <conv:InverseBoolConverter x:Key="InverseBool" />
        <conv:CanSelectIfWatchedConverter x:Key="CanSelectIfWatched" />
        <conv:CanSelectIfReleasedConverter x:Key="CanSelectIfReleased" />
    </UserControl.Resources>
    
    <DockPanel>
        <StackPanel DockPanel.Dock="Top">
            <DockPanel Margin="2,2,2,0">
                <Button DockPanel.Dock="Right" Background="Transparent" BorderThickness="0" Width="20" Height="20" Padding="0"
                        Command="{Binding SidePanel.Close}" FontSize="16" Content="{x:Static resx:Messages.RemoveIcon}"
                        HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                        HorizontalAlignment="Right" VerticalAlignment="Top" />
            </DockPanel>
            
            <TextBlock Text="{Binding FormTitle}" FontSize="24" Margin="5,0,5,5" HorizontalAlignment="Stretch" TextWrapping="Wrap" />
        </StackPanel>

        <Border BorderBrush="LightGray" BorderThickness="0,1,0,0" HorizontalAlignment="Stretch" DockPanel.Dock="Bottom">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="5"
                        Visibility="{Binding CanSaveOrCancelChanges, Converter={StaticResource BoolToVisibility}}">
                <StackPanel.Resources>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Margin" Value="2" />
                        <Setter Property="Padding" Value="5,2" />
                        <Setter Property="Width" Value="100" />
                    </Style>
                </StackPanel.Resources>

                <Separator />
                <Button Content="{x:Static resx:Messages.Cancel}" Command="{Binding Cancel}" />
                <Button Content="{x:Static resx:Messages.Save}" Command="{Binding Save}" />
            </StackPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel DataContext="{Binding Movie}" Margin="10">
                <StackPanel.Resources>
                    <local:BindingProxy x:Key="MovieFormItem" Data="{Binding}" />
                </StackPanel.Resources>
                
                <Image Source="{Binding Poster}" />
                
                <TextBlock Margin="5" HorizontalAlignment="Center">
                    <Hyperlink NavigateUri="{Binding ImdbLink}" local:Properties.IsExternal="True">
                        <Hyperlink.Inlines>
                            <Run Text="{Binding ImdbLink}" />
                        </Hyperlink.Inlines>
                    </Hyperlink>
                </TextBlock>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <CheckBox x:Name="IsWatchedCheckBox" Content="{x:Static resx:Messages.Watched}" IsChecked="{Binding IsWatched}"
                              IsEnabled="{Binding Converter={StaticResource CanSelectIfWatched}}" />
                    <CheckBox x:Name="IsNotReleasedCheckBox" Content="{x:Static resx:Messages.NotReleased}" Margin="10,0,0,0"
                              IsChecked="{Binding Path=IsReleased, Converter={StaticResource InverseBool}}"
                              IsEnabled="{Binding Converter={StaticResource CanSelectIfReleased}}" />
                </StackPanel>

                <DockPanel>
                    <TextBlock Text="{x:Static resx:Messages.Name}" Style="{StaticResource InputTitleStyle}" />
                    <Button Command="{Binding AddTitle}" Style="{StaticResource AddButtonStyle}" />
                </DockPanel>

                <ItemsControl ItemsSource="{Binding Titles}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vmf:TitleFormItem}">
                            <DockPanel>
                                <Button Command="{Binding Path=Data.RemoveTitle, Source={StaticResource MovieFormItem}}"
                                        CommandParameter="{Binding}" Style="{StaticResource RemoveButtonStyle}" />
                                <TextBox Text="{Binding Path=Name, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource InputTextBoxStyle}" />
                            </DockPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <DockPanel>
                    <TextBlock Text="{x:Static resx:Messages.OriginalName}" Style="{StaticResource InputTitleStyle}" />
                    <Button Command="{Binding AddOriginalTitle}" Style="{StaticResource AddButtonStyle}" />
                </DockPanel>

                <ItemsControl ItemsSource="{Binding OriginalTitles}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vmf:TitleFormItem}">
                            <DockPanel>
                                <Button Command="{Binding Path=Data.RemoveOriginalTitle, Source={StaticResource MovieFormItem}}"
                                        CommandParameter="{Binding}" Style="{StaticResource RemoveButtonStyle}" />
                                <TextBox Text="{Binding Path=Name, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource InputTextBoxStyle}" />
                            </DockPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,1,0">
                        <TextBlock Text="{x:Static resx:Messages.Year}" Style="{StaticResource InputTitleStyle}" />
                        <TextBox Text="{Binding Path=Year, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"  />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="1,0,0,0">
                        <TextBlock Text="{x:Static resx:Messages.Kind}" Style="{StaticResource InputTitleStyle}" />

                        <ComboBox ItemsSource="{Binding Path=AllKinds}" FontSize="11"
                                  SelectedItem="{Binding Path=Kind, UpdateSourceTrigger=PropertyChanged}" Margin="0,2">
                            <ComboBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:KindViewModel}">
                                    <TextBlock Text="{Binding Name}" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <TextBlock Text="{x:Static resx:Messages.Imdb}" Style="{StaticResource InputTitleStyle}" />
                <TextBox Text="{Binding Path=ImdbLink, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource UrlInputTextBoxStyle}" />

                <TextBlock Text="{x:Static resx:Messages.PosterUrl}" Style="{StaticResource InputTitleStyle}" />
                <TextBox Text="{Binding Path=PosterUrl, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource UrlInputTextBoxStyle}" />
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
