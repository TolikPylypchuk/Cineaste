@using Cineaste.Client.Navigation
@inject IPageNavigator PageNavigator

@code {
    protected override void OnInitialized() {
        this.PageNavigator.GoToPageWithCurrentReturnUrl(CineastePage.Login, forceReload: true);
    }
}
